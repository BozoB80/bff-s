import { Text, View } from "react-native";
import { Header } from "@/src/components";

const Home = () => {
	return (
		<View>
			<Header />
			<Text>Hello there also</Text>
		</View>
	);
};

export default Home;

import { Text, View } from "react-native";
import { Header } from "@/src/components";

const Messages = () => {
	return (
		<View>
			<Header />
			<Text>messages</Text>
		</View>
	);
};

export default Messages;
